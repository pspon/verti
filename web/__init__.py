"""FastAPI + HTMX web UI for the Verti Garden Planner.

This UI is built on the same ``verti.repository`` data layer as the legacy
Streamlit app, so the two can run side by side during the migration.
"""
