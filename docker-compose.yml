services:
  web:
    build: .
    container_name: verti
    ports:
      - "8080:8080"
    environment:
      VERTI_DB_PATH: /data/verti.db
    volumes:
      # Persist the database across container restarts / redeploys.
      - verti-data:/data
    restart: unless-stopped

volumes:
  verti-data:
